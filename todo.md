# Infinity HQ - Local MVP Todo

1. Initialize MVP (completed): express + ws + static frontend (server.js, public/index.html)
2. Add authentication (next): JWT + WebAuthn for hardware keys
3. Add encrypted rooms: client-side key generation + server relay
4. Add persistence: Postgres with encrypted columns for secrets
5. CI & tests: basic unit tests for API endpoints
6. Production hardening & Docker image push once billing active

Use this list to track progress. Focus now: authentication and encrypted rooms.
